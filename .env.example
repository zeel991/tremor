# ---- chain RPCs (public endpoints work; private ones are more reliable) ----
BASE_RPC_URL=https://mainnet.base.org            # forked locally by anvil for the demo; read-only
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org    # public testnet deployment target
# ---- deployer (Base Sepolia only; never an anvil key, never a key that held mainnet value) ----
DEPLOYER_PK=
ETHERSCAN_API_KEY=
# ---- backend ----
RPC_URL=http://127.0.0.1:8545
DEPLOYMENT_JSON=../contracts/deployments/31337.json
DATABASE_URL=sqlite://tremor.db
PORT=8787
CORS_ORIGIN=http://localhost:3000
# ---- web ----
NEXT_PUBLIC_API_URL=http://localhost:8787
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_CHAIN_ID=31337
