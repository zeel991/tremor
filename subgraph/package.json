{
  "name": "tremor-subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-local": "graph create --node http://127.0.0.1:8020 tremor && graph deploy --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001 tremor"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.0",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
