# Tremor contributor instructions

The canonical repository instructions are in [`AGENTS.md`](./AGENTS.md). Read that file, followed by
`ARCHITECTURE.md`, `DESIGN.md`, `contracts/CONTRACTS.md`, and `docs/TASKS.md` before changing the project.

Keeping one source of truth prevents safety invariants and verification counts from drifting between
agent-specific instruction files.
